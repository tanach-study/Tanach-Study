language: node_js
node_js:
  - 8.3.0
cache:
  directories:
    - node_modules
install:
  - npm install
  - sudo pip install awscli
script:
  - npm run build
deploy:
  - provider: script
    script: bash scripts/deployment_travis.sh
    skip_cleanup: true
    on:
      all_branches: true
